{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/pgirl/genomic-intelligence/master/assets/schema_input.json",
    "title": "pgirl/genomic-intelligence pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "fasta": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^([\\S\\s]*\\/)?[^\\s\\/]+\\.(fa(sta)?|fna)(\\.gz)?$",
                "errorMessage": "FASTA file must be provided, cannot contain spaces and must have extension '.fasta', '.fa', '.fna' (optionally gzipped)"
            },
            "metadata": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^([\\S\\s]*\\/)?[^\\s\\/]+\\.(tsv|csv)$",
                "errorMessage": "Metadata file must be provided in TSV or CSV format"
            }
        },
        "required": ["sample", "fasta", "metadata"]
    }
}
