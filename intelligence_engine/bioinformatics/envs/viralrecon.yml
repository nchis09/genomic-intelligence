name: pgirl_viralrecon
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  # Nextflow runtime
  - nextflow>=23.10.0
  - openjdk>=17
  # nf-core tools (for downloading/launching pipelines)
  - nf-core>=2.13
  # Container runtime - viralrecon uses containers per process
  # Install Docker or Singularity separately; conda provides singularity:
  # - singularity>=3.8  # uncomment if no Docker available
  # Utility
  - git
